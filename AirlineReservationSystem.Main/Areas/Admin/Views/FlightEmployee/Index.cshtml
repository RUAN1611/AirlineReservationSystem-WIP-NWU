@model IEnumerable<FlightEmployee>

<div class="container">
    <div class="row">
        <div class="col-6 mt-3">
            <h2 class="text-primary">
                List of Assignments
            </h2>
        </div>
        <div class="col-6 text-end mt-3">
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Add Assignment
            </a>
        </div>
    </div>
    <table class="table table-bordered table-striped my-4">
        <thead>
            <tr>
                <th>
                    Flight ID and Departure Time
                </th>
                <th>
                    Employee ID and Full Name
                </th>
                <th>

                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td>
                        @obj.FlightId - @obj.Flight.DepartureTime
                    </td>
                    <td>
                        @obj.EmployeeId - @obj.Employee.FirstName @obj.Employee.LastName
                    </td>
                    <td>
                        <div class="btn-group w-100 align-content-center justify-content-center" role="group">
                            <a asp-action="Edit" asp-route-flightId="@obj.FlightId" asp-route-employeeId="@obj.EmployeeId" class="btn btn-secondary mx-2">
                                <i class="bi bi-pencil-square"></i> Edit
                            </a>
                            <a asp-action="Details" asp-route-flightId="@obj.FlightId" asp-route-employeeId="@obj.EmployeeId" class="btn btn-info mx-2">
                                <i class="bi bi-info-circle"></i> Details
                            </a>
                            <a asp-action="Delete" asp-route-flightId="@obj.FlightId" asp-route-employeeId="@obj.EmployeeId" class="btn btn-danger mx-2">
                                <i class="bi bi-x-square"></i> Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>